# macOS only, one-time per machine, before installing:  brew install libomp
# xgboost and lightgbm link against libomp.dylib but don't ship it, and macOS
# has no system OpenMP. Without it `import lazypredict` crashes.

numpy
pandas<3
scipy
pyarrow

scikit-learn
lazypredict>=0.3
xgboost
lightgbm

matplotlib
seaborn
